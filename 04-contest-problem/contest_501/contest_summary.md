1）第一题直接秒
2）第二题：1）跳过所有不是小写字母的分隔符，找到第一个单词的起始位置；2）扩展字符：小写字母直接加入，'-'要求两侧字母都为小写字母；3）遇到其他字符直接停止扩展；3）用哈希表统计每一个合法单词出现的次数，在与queries匹配
3）第三题：动态规划，dp[i]表示i可以替换的最小数字， dp[k*nums[i]]=min(dp[k*nums[i]],dp[nums[i]]);遍历所有值为nums[i]倍数的数,替换成最小nums[i]；