
> Problem: [542. 01 矩阵](https://leetcode.cn/problems/01-matrix/description/)

[TOC]

# 思路

多源bfs

# 解题过程


# 复杂度

- 时间复杂度: $O(m*n)$
- 空间复杂度: $O(m*n)$



# Code
```C++ []
class Solution {
public:
    vector<vector<int>> updateMatrix(vector<vector<int>>& mat) {
        vector<pair<int,int>>dir={{-1,0},{0,-1},{1,0},{0,1}};
        int m=mat.size(),n=mat[0].size();
        vector<vector<int>>ans(m,vector<int>(n,-1));
        queue<pair<int,int>>q;
        for(int i=0;i<m;i++){
            for(int j=0;j<n;j++){
                if(mat[i][j]==0){
                    ans[i][j]=0;
                    q.push({i,j});
                }
            }
        }
       
        while(!q.empty()){
                auto [x,y]=q.front();
                q.pop();
                for(auto [dx,dy]:dir){
                    int nx=x+dx,ny=y+dy;
                    if(nx>=0&&nx<m&&ny>=0&&ny<n&&ans[nx][ny]==-1){
                        ans[nx][ny]=ans[x][y]+1;
                        q.push({nx,ny});
                    }
                }
        }
        return ans;
    }
};
```
  
